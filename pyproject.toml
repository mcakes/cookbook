[project]
name = "cookbook"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-slugify>=8.0",
    "pyyaml>=6.0.3",
    "recipe-scrapers>=15.12.0",
    "requests>=2.34.2",
]
